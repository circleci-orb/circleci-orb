steps:
  - checkout
  - run:
      name: install latest go via homebrew
      command: |
        brew upgrade https://github.com/Homebrew/homebrew-core/raw/master/Formula/go.rb || brew install https://github.com/Homebrew/homebrew-core/raw/master/Formula/go.rb || true
